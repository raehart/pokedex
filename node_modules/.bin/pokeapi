#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pokeapi/bin/pokeapi" "$@"
  ret=$?
else 
  node  "$basedir/../pokeapi/bin/pokeapi" "$@"
  ret=$?
fi
exit $ret
